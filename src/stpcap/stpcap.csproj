<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="stpcap.Tests" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\shared\shared.csproj" />
  </ItemGroup>

  <!-- The Linux XRecord backend shells out to this helper; ship it beside the binary. -->
  <ItemGroup>
    <None Include="stprec_helper.py" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
