using Xunit;

namespace imgfnd.Tests;

// imgfnd tests require a live desktop + reference images.
// Placeholder test project — add integration tests when CI has display access.

public class ImgfndTests
{
    [Fact]
    public void Placeholder_TestProjectBuilds()
    {
        Assert.True(true);
    }
}
