# outcap.Tests

Tests for the audio+video merge tool.

- **Run**: `dotnet test src/outcap.Tests/outcap.Tests.csproj`
- **Covers**: Options parser defaults, global propagation, per-stream overrides
- **Prerequisites**: none (parser tests only)
